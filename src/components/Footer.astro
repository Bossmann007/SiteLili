---
import { site } from '../data/site';
import Logo from './Logo.astro';
---

<footer class="relative overflow-hidden border-t border-line bg-brand-cream">
  <div class="pointer-events-none absolute inset-0 opacity-[0.03]" style="background-image:url('/brand/pattern.png');background-size:360px auto;" aria-hidden="true"></div>
  <div class="container-site relative grid gap-10 py-12 md:grid-cols-[1.4fr_1fr] md:py-16">
    <div data-footer-reveal>
      <Logo variant="footer" />
      <p class="mt-4 max-w-md prose-site text-sm">
        Medicina de família e comunidade com foco em estilo de vida, longevidade e saúde integral para mulheres 40+.
      </p>
      <p class="mt-4 text-sm text-ink-soft">
        {site.crm} · {site.rqe}
      </p>
    </div>

    <div class="space-y-4 text-sm text-ink-soft" data-footer-reveal>
      <p>
        <span class="block font-medium text-ink">Endereço</span>
        {site.address.full}
      </p>
      <p>
        <span class="block font-medium text-ink">Contato</span>
        <a href={`tel:${site.phoneTel}`} class="hover:text-gold-deep">{site.phone}</a>
        <br />
        <a href={`mailto:${site.email}`} class="hover:text-gold-deep">{site.email}</a>
      </p>
      <p>
        <span class="block font-medium text-ink">Redes</span>
        <a href={site.instagram.personal} class="hover:text-gold-deep" target="_blank" rel="noopener noreferrer">
          {site.instagram.personalHandle}
        </a>
        <br />
        <a href={site.instagram.clinic} class="hover:text-gold-deep" target="_blank" rel="noopener noreferrer">
          {site.instagram.clinicHandle}
        </a>
      </p>
    </div>
  </div>

  <div class="border-t border-line/80">
    <div class="container-site flex flex-col gap-2 py-6 text-xs text-ink-soft sm:flex-row sm:items-center sm:justify-between" data-footer-reveal>
      <p>© {new Date().getFullYear()} {site.shortName}. Todos os direitos reservados.</p>
      <p>
        As informações deste site têm caráter informativo e não substituem consulta médica.
      </p>
    </div>
  </div>
</footer>
